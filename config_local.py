"""Local development configuration."""

# Set to True to skip login (auto-login as demo user)
SKIP_LOGIN = True

# Demo user info (used when SKIP_LOGIN is True)
DEMO_USER = {
    "id": "demo-user-001",
    "email": "demo@local.dev",
    "full_name": "Demo User",
    "is_active": True
}

